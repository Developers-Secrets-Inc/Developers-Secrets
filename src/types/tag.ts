/**
 * Type représentant un tag/concept qui peut être associé à un article
 * Par exemple: "python", "orienté objet", "décorateurs", etc.
 */
export type Tag = {
  id: string
  name: string // Le nom du tag (ex: "python", "orienté objet")
}
